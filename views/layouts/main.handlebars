<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Project 02</title>
    <!-- Font-Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.1/css/all.min.css" rel="stylesheet">
    <!-- Bootstrap -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/css/style.css" />
      <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.1/jquery.min.js"></script>
  </head>

  <body class="container-xxl my-md-4">
    <header class="p-3 text-white text-center">
      <nav class="navbar navbar-expand-sm navbar-dark">
        <button class="navbar-toggler navbar-toggler-right"
        type="button" data-toggle="collapse" data-target="#navmenu"
        aria-controls="navmenu" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <a class="navbar-brand" href="/">
          <i class="fa-solid fa-chart-pie"></i>
          <span>Project 2</span>
        </a>
        <div class="collapse navbar-collapse justify-content-between" id="navmenu">
          <ul class="navbar-nav">
            <!-- Conditionally render accountCreation or account links -->
            {{#if logged_in}}
              <li class="nav-item active">
                <a class="nav-link" href="/account">Account</a>
              </li>
                <li class="nav-item active">
                    <a class="nav-link" href="/taxInputs">Tax Report</a>
                </li>
            {{else}}
              <li class="nav-item">
                <a class="nav-link" href="/accountCreation">Make an Account</a>
              </li>
            {{/if}}
          </ul>
        </div>
        <!-- Conditionally render login or logout links -->
        <div class="ms-auto">
          {{#if logged_in}}
            <button class="btn btn-link nav-link link-light" 
            data-toggle="tooltip" data-placement="bottom" title="Logout" id="logout">
              <i class="fa-solid fa-user"></i>
            </button>
          {{else}}
            <a class="nav-link link-light" data-toggle="tooltip" data-placement="bottom" title="Login" href="/login">
              <i class="fa-regular fa-user"></i>
            </a>
          {{/if}}
        </div>
      </nav>
    </header>

    <main>
      <!-- Render the sub layout -->
      {{{body}}}
    </main>

    {{#if logged_in}}
      <script src="/js/logout.js"></script>
    {{/if}}
  </body>
  <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
    crossorigin="anonymous"
  ></script>
</html>